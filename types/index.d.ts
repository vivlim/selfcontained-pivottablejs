/**
 * Injects data into a self-contained pivottable.js html file.
 * @param {unknown} data
 * @param {unknown} options
 * @param {string} targetFilename
 */
export declare function inject(data: unknown, options: () => any, targetFilename: string): void;